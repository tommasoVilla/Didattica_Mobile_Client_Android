<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.uniroma2.dicii.sdcc.didatticamobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.CourseDetailActivity" />
        <activity android:name=".activity.FindCoursesActivity" />
        <activity android:name=".activity.HomeActivity" />
        <activity android:name=".activity.RegistrationCompletedActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RegistrationActivity" />
        <activity android:name=".activity.CoursesActivity" />
        <activity android:name=".activity.AddCourseActivity" />
        <activity android:name=".activity.AddCourseScheduleActivity" />
        <activity android:name=".activity.AddCourseDescriptionActivity" />
        <activity android:name=".activity.AddExamActivity" />
        <activity android:name=".activity.ExamsActivity" />
        <activity android:name=".activity.TeachingMaterialActivity" />
        <activity android:name=".activity.CourseTeachingMaterialsActivity"></activity>
    </application>

</manifest>